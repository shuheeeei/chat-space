.side-bar
  .side-bar__user
    .side-bar__user__name
      %p
        = current_user.name
    .side-bar__user__edit-icon
      %i.list
        = link_to do
          = fa_icon 'pencil-square-o', class: 'icon'
      %i.list
        = link_to edit_user_path(current_user) do
          = fa_icon 'cog', class: 'icon'

  = render partial: "group-menus", locals: { group_name: "神", message_preview: "sdlkfjsdlf" }
  = render partial: "group-menus", locals: { group_name: "aaaaaaaaaaa", message_preview: "まだメッセージはありません。" }
  = render partial: "group-menus", locals: { group_name: "sampleグループ", message_preview: "まだメッセージはありません。" }
